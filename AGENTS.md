# 邮学伴项目工作规范

本文是 `bupt_study_assistant` 的项目级开发说明，供开发者和代码代理共同使用。目标是让项目能够脱离 `D:\Bots` 下的其他目录独立开发、测试、演示和发布。

## 1. 项目定位与边界

- 项目根目录：`D:\Bots\bupt_study_assistant`。
- 产品名称：邮学伴，副标题“北邮人的学习与生活助手”。
- `web/` 是 Vue 3 + TypeScript 前端，也是课程设计主要展示界面。
- `src/` 保留 NoneBot 业务层，供 AI、DDL、课程、校园数据和课程结束后的 QQ 机器人模式复用。
- 项目不得运行时引用 `D:\Bots\new_bot`、NapCat 安装目录或其他兄弟目录。
- 普通用户界面不得要求输入 `/ddl`、`/course` 等机器人命令；优先使用按钮、表单、筛选器和自然语言。
- 第二课堂只允许查询、展示和订阅，不实现报名、签到或退选。

## 2. 目录与事实来源

```text
bupt_study_assistant/
├─ web/                         Vue 前端和本地开发 API
│  ├─ src/App.vue               页面和交互入口
│  ├─ src/course-import.ts       XLS/XLSX/CSV 课表解析
│  ├─ src/types.ts               前端 DTO
│  ├─ src/styles.css             响应式样式
│  └─ dev-api.ts                 服务端 AI、教务与校园代理
├─ src/                          NoneBot 领域、服务、仓储和插件
├─ config/ai_routes.toml         AI 模型路由事实来源
├─ tests/                        后端回归测试
├─ docs/                         架构、定制和遗留说明
├─ .env.example                  无凭据的配置模板
├─ run-web.cmd                   Windows 推荐启动入口
└─ run-web.ps1                   可指定端口的 PowerShell 入口
```

以下内容必须保持单一事实来源：

- AI 供应商、模型和回退顺序：`config/ai_routes.toml`。
- Python 依赖：`pyproject.toml` 和 `uv.lock`。
- 前端依赖：`web/package.json` 和 `web/pnpm-lock.yaml`。
- 前端数据结构：`web/src/types.ts`。
- 产品架构和接口规划：`docs/web-frontend-design.md`。

## 3. 独立运行方式

### 3.1 前端

首次克隆后安装依赖：

```powershell
cd .\web
pnpm install
cd ..
```

Windows 推荐启动：

```powershell
.\run-web.cmd
```

需要指定端口时：

```powershell
pwsh -NoLogo -NoProfile -File .\run-web.ps1 -Port 5174
```

默认地址为 `http://127.0.0.1:5173/`。端口被占用时不要结束来源不明的进程，改用另一个端口。

### 3.2 可选在线能力

前端基础功能和文件课表导入不依赖 NoneBot。在线 AI、按班级导入和校园查询由 `web/dev-api.ts` 在服务端处理：

1. 复制 `.env.example` 为 `.env`；
2. 把真实凭据放在已忽略的 `secrets/` 中；
3. 配置 `YOUXUEBAN_API_KEY_FILE`、`YOUXUEBAN_JWGL_COOKIE_FILE` 与校园只读会话；
4. 不得把密钥、Cookie 或完整上游错误返回给浏览器。

AI 不可用时应明确显示在线服务错误，不能用固定文案伪装回答或切换到本地助手。教务会话缺失或失效时应说明原因，不能显示成“暂无课程”。校园官网外链只能说明用户可以访问官网，不代表服务端已取得在线数据；页面必须分别展示信息门户和第二课堂的在线、缓存、成功空结果或失效状态。

### 3.3 NoneBot 后端

需要运行保留的机器人业务层时：

```powershell
uv sync --frozen
nb run
```

前端基础页面不应依赖该进程。切回 QQ 模式时再配置 OneBot/NapCat，不把 NapCat 作为 Web 版必需组件。

## 4. 标准开发流程

每次修改遵循同一闭环：

1. **确认范围**：明确是解释、诊断、实现还是发布，不擅自扩大任务。
2. **检查现状**：阅读相关文件、`git status` 和配置模板；保留用户已有修改。
3. **定位根因**：从界面行为追到状态、API、service 和 repository，不通过占位 toast 假装修复。
4. **最小实现**：只修改根因相关层，并保持前端、API 和类型契约一致。
5. **处理失败态**：区分未配置、鉴权失败、网络失败、解析失败、成功空结果和离线降级。
6. **自动化验证**：运行类型检查、构建和相关后端测试。
7. **真实交互验收**：涉及页面时在桌面和手机宽度实际点击，不只看编译结果。
8. **清理现场**：停止测试服务器，删除临时测试文件，不遗留额外进程。
9. **同步文档**：运行方式、接口、配置或架构变化时更新 README 和相关文档。
10. **交付说明**：说明完成内容、验证证据、仍需配置的外部条件和文件位置。

## 5. 前端实现约定

- Vue 状态、组件属性和 API 数据必须有 TypeScript 类型，避免无边界的 `any`。
- 当前 MVP 使用 `localStorage` 保存任务、课程、已读状态、助手会话和设置；不得把凭据或校园认证信息写入 `localStorage`。
- 修改持久化结构时提升存储 key 版本，并保留旧 key 迁移入口；对图片等大字段捕获配额异常，限制单文件大小和数量，不能让一次上传破坏全部状态保存。
- 浏览器不能直接持有 API Key 或 Cookie。敏感上游请求只能由服务端代理。
- 文件课表在浏览器本地解析，支持教务系统原始 XLS/XLSX 和规范 CSV；解析失败要给出具体列或格式错误。
- 课表节次统一使用 `startSection` 与 `endSection`；文件中的 `1-6`、`1～6`、`1至6` 等范围必须同时识别起止节次，课程时间使用结束节次推导，不得只取第一个数字。
- 周课表中的表头、节次标签、空白格和课程块全部显式设置 CSS Grid 行列。禁止混用自动排布和显式排布，否则跨节课程会占用格子并把表头、时间列推移。连续课程使用一个跨行课程块，只渲染一份课程信息。
- 班级课表查询必须使用已配置的教务会话，不能用演示课程冒充查询成功。
- 校园条目必须来自信息门户、第二课堂或其真实缓存，并保存具体 `url`；不得添加演示条目。
- 信息门户和教务 Cookie 会自然过期；HTTP 200 后仍要检查统一认证登录页、系统提示页和业务表单。第二课堂返回空数组表示“在线但当前无活动”，不得显示成连接失败。
- 危险操作使用页面内二次确认。禁止使用 `window.confirm()` 破坏交互一致性。
- 提醒时间使用自由数字输入，0 表示到点提醒；前端限制和后端校验必须一致。
- “正在上课”和“下一节课”必须基于当前分钟分别计算：`start <= now < end` 为正在上课，`start > now` 才是下一节；已经结束的第一节课不得继续作为下一节显示。
- 顶部全局栏只放全局操作；页面名称由页面自身的 `h1` 表达，避免全局标题和内容标题重复。
- 欢迎动画必须使用基于秒的速度和 `requestAnimationFrame`，初始方向、速度随机，处理墙体与圆间碰撞。视口变化时按新宽高缩放位置并限制半径、速度，不能只裁剪坐标；动画元素数量和半径必须设置上下限，避免高倍缩放卡死或小视口近似静止。
- 页面必须支持浅色/深色、键盘操作和移动端布局，状态不能只依赖颜色表达。
- 新增功能不得要求学生记忆机器人命令。

### 5.1 AI 助手界面与能力契约

- 助手的供应商、模型、思考、联网和文件能力统一从 `config/ai_routes.toml` 读取，前端不能硬编码另一套能力表。
- 只有模型明确声明支持时才显示思考、联网或上传控件；每次请求仍由服务端复核，不能信任浏览器传来的 MIME、大小或思考参数。
- 思考开关属于单个对话状态；多会话要保存活动会话、标题、消息和更新时间，切换对话时不得串用草稿或附件。
- 图片等多模态内容使用 OpenAI 兼容的 `image_url` 消息结构，客户端和服务端同时限制 MIME、数量、大小和 data URL。当前只允许配置声明的 PNG、JPEG、WebP。
- 不展示或持久化上游 `reasoning_content`；界面只展示最终回答和用户可理解的运行状态。
- 学习问答提示词应支持概念讲解、分步骤解题、图片分析和自检方法，同时遵守学术诚信，不声称执行了未实际执行的操作。

## 6. 后端分层约定

- `plugins/`：协议适配、参数拆解、权限检查和结果发送，保持轻量。
- `services/`：时间解析、课程解析、AI 路由、校园访问和业务规则。
- `repositories/`：SQLite 初始化、迁移和数据访问。
- `domain/`：消息、权限、命令和 AI DTO。
- service 不反向依赖 plugin；SQL 不放进前端或 matcher。
- 用户、群和全局数据必须保持作用域隔离。
- 外部 HTTP 200 不等于业务成功；还要验证登录状态、响应形状和合理条目数。
- 鉴权失败、解析失败和成功空结果必须使用不同错误语义。
- 新增数据库字段或行为时同步迁移和回归测试。

## 7. 验证命令

### 7.1 前端必做

```powershell
cd .\web
.\node_modules\.bin\vue-tsc.cmd --noEmit -p .\tsconfig.app.json
.\node_modules\.bin\tsc.cmd --noEmit -p .\tsconfig.node.json
.\node_modules\.bin\vite.cmd build
```

也可以在正常交互终端运行：

```powershell
pnpm run build
```

涉及界面或交互时还必须验证：

- 常规桌面宽度；
- 390px 左右手机宽度；
- 无横向溢出；
- 浏览器控制台无错误；
- 表单、二次确认、导入和持久化真实生效。
- 浏览器页面整体滚动条可按设计隐藏，但必须保留滚动能力；课表和对话列表等内部滚动区域不得因此失效。
- 响应式测试使用临时视口覆盖时，结束前恢复默认视口；检查 `scrollWidth === clientWidth` 作为无横向溢出的证据之一。
- 文件上传自动化要先监听 `filechooser`，再点击真实可见的上传按钮或标签，最后使用绝对路径设置文件；隐藏 input 直接点击在部分浏览器中不会触发选择事件。

### 7.2 后端按需

```powershell
uv lock --check
.\.venv\Scripts\ruff.exe check --no-cache .
.\.venv\Scripts\pytest.exe -q -p no:cacheprovider
```

外部 AI、教务和校园源探针可能产生真实请求或费用，只有任务明确需要时才运行，并且不得输出凭据。

模型或能力配置变更后，同时更新路由测试的预期模型。仅通过普通聊天探针不足以证明思考模式可用；若本次任务涉及思考参数，使用最小提示分别验证对应参数的真实 HTTP 成功状态，输出只包含模型、状态码和是否有正文。

## 8. PowerShell 与文件安全

- 搜索文件优先使用 `rg --files`，搜索文本优先使用 `rg`。
- 修改源码使用可审查的补丁，不使用临时脚本大范围重写。
- PowerShell 调用路径时使用 `-LiteralPath`；调用原生程序时使用参数数组或清晰的独立参数。
- 不使用 `git reset --hard`、`git checkout --` 或递归删除来清理用户改动。
- 不结束来源不明的 Node、Python、NapCat 或浏览器进程。
- 测试服务器使用独立端口，并在验收后停止。
- 结束测试服务器后检查该端口已不再监听；浏览器控制台、临时视口、测试标签页和本地上传文件也要一并清理。
- 工作区可能已有用户修改；只提交当前任务相关内容。

## 9. 凭据与 GitHub 发布

以下内容永远不得提交：

- `.env`、`secrets/`、密码、API Key、Cookie、Bearer token；
- `data/`、`logs/`、`backups/`、SQLite 数据库；
- `.venv/`、`__pycache__/`、`.pytest_cache/`；
- `web/node_modules/`、`web/dist/`、`.vite/`、`*.tsbuildinfo`；
- 临时课表、截图、测试输出和本机绝对用户路径。

发布前执行：

1. 核对 `.gitignore`；
2. 用文件名和内容模式扫描凭据，但不要把疑似值打印到终端；
3. 检查 `git diff --cached --check`；
4. 检查暂存文件树中没有缓存、数据库或密钥目录；
5. 使用 GitHub noreply 邮箱提交；
6. 默认创建私有仓库，只有用户明确要求时才公开；
7. 推送后核对远端可见性、默认分支、提交 SHA 和文件树。

当前远端为 `https://github.com/hello-notch/bupt-study-assistant`，默认分支为 `main`。后续是否推送由用户请求决定，不因本地修改自动发布。

## 10. 完成标准

任务只有同时满足以下条件才算完成：

- 用户请求的行为真实可用，而非仅修改文案或 toast；
- 在线、离线、空结果和错误状态表达准确；
- TypeScript/构建和相关测试通过；
- 桌面与移动端交互经过实际验收；
- 没有泄露凭据、覆盖用户数据或依赖兄弟目录；
- 没有遗留测试服务器、临时文件或未说明的外部配置；
- README、配置模板和实现保持一致。

## 11. 已验证的易错点与回归基线

以下问题在真实浏览器中出现过，修改相关代码时应优先回归：

1. **课表错位**：CSS Grid 自动排布会避开已显式占位的跨节课程，导致星期表头和节次标签漂移。所有网格元素显式定位后，`1-6` 课程应表现为 `grid-row: 2 / span 6` 一类跨行块。
2. **节次范围丢失**：导入值只取第一个数字会把 `1-6` 误成单节课。预览和最终课程卡都要显示同一个结束节次。
3. **在线状态误判**：官网链接可打开、HTTP 200、缓存有数据三者都不等于在线鉴权有效。状态必须以最终 URL、登录页特征和业务响应形状为准。
4. **当前课程误判**：查找“结束时间晚于现在”的第一项会把已开课课程和下一节混在一起。正在上课与未来课程必须拆成两个条件。
5. **动画缩放失真**：只在初始化计算半径和速度会导致浏览器缩放后圆球过大、卡边或几乎不动。`ResizeObserver` 必须同步重标定位置、半径和速度。
6. **能力显示失真**：前端无条件显示思考、联网、上传，会让不支持的模型出现无效按钮。运行时配置必须作为 UI 和服务端校验的共同依据。
7. **持久化配额**：对话图片直接写入 `localStorage` 很容易达到浏览器限制。始终保持附件上限、捕获写入失败，并避免保存无用的中间副本。
8. **验证假阳性**：类型检查和构建通过不代表交互正确。课表对齐、圆球移动与换色、空课表换周、删除二次确认、多会话刷新保留、思考切换、图片预览和 390px 无溢出都应通过真实页面证据验证。

最近一次完整回归基线：前端两项 TypeScript 检查与 Vite 构建通过，Python 测试 53 项通过；桌面端和 390px 视口无横向溢出、控制台无错误；`DeepSeek-V4-Flash-Vision-Exp` 普通聊天和思考参数均取得真实 HTTP 200。后续测试数量变化时以当前测试集为准，不要把数字写进产品界面。
